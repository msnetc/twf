<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.taimeitech.platform.dao.multids.BookMultiDsDao">

	<!--多数据源-->
	<select id="listBook1" resultType="Book">
		SELECT * FROM book ORDER by book_id LIMIT 0,20
	</select>

	<select id="listBook2" resultType="Book">
		SELECT * FROM book ORDER by book_id LIMIT 0,20
	</select>

</mapper>